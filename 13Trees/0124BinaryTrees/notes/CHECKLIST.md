# Binary Trees (BST, iterative) — Course: Java DSA + LeetCode (Scott Barrett, Udemy)

Verified against the real course curriculum (course id 4218796, fetched directly). This is the "Trees" chapter specifically — the iterative BST build. Recursive BST (with delete) and Tree Traversal (BFS/DFS) are separate chapters in the real course — see `0132 - Recursive Binary Search Trees` and `0133 - Tree Traversal`, don't redo them here.

Core build, exact lecture order:
- [ ] Trees: Intro & Terminology
- [ ] Binary Search Trees: Example
- [ ] BST: Big O
- [ ] BST: Constructor
- [ ] BST: Insert
- [ ] BST: Contains

You already have zero Tree coverage elsewhere in `05. DSA` — this was the highest-priority new section (see `00000 - REVISION HUB/GAPS_AND_ROADMAP.md`). Cross-reference `00000 - REVISION HUB/CHEATSHEETS/Trees.md` for Java + Kotlin templates.

Starter skeleton: `code/BinaryTree.java`.

`code/Reference_ivanzykov_BinarySearchTree.java` is another student's repo for this same course, pulled in at your request. It actually covers this chapter's insert/contains AND all of `0133`'s traversal methods (BFS, DFS pre/post/in-order) in one file — but has no delete, so it's no help for `0132`. Write your own first, then diff.
