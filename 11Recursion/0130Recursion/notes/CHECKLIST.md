# Recursion — Course: Java DSA + LeetCode (Scott Barrett, Udemy)

Verified against the real course curriculum — it's intentionally tiny, just 3 lectures:

- [ ] Recursion: Intro
- [ ] Call Stack
- [ ] Factorial

That's it from the course. You already have a much deeper unstructured `Recursion/` folder at the root of `05. DSA` (factorial, fibonacci, power, tiling, sorted-check, binary string generator, remove duplicates) — that's real prior work, more thorough than this course chapter, not a gap. Re-port it here for fast recall practice if you want it numbered/tracked, but don't feel like this 3-lecture chapter is "the real recursion topic" — it's just the on-ramp the course uses before Recursive BST (`0132`).

This connects directly to Backtracking and Tree/Graph DFS — recursion is the foundation those build on, see `00000 - REVISION HUB/CHEATSHEETS/Patterns.md` (Backtracking section) and `Trees.md`/`Graphs.md`.
