# Array — Interview / LeetCode Exercises — Course: Java DSA + LeetCode (Scott Barrett, Udemy)

Verified against the real course curriculum — this is the course's final array-focused chapter, separate from your existing `0016`/`0045`–`0047` array work. Some overlap is likely (e.g. "Remove Duplicates" may be near-identical to `0047`) — solve here first from memory, then diff against the old solution rather than assuming they're the same problem.

- [ ] Remove Element
- [ ] Find Max Min
- [ ] Find Longest String
- [ ] Remove Duplicates
- [ ] Max Profit — this is "Best Time to Buy and Sell Stock," a single-pass min-tracking problem, extremely common in interviews
- [ ] Rotate (array rotation — connects to your existing `0035` Transpose/Rotate Matrix intuition, one dimension down)
- [ ] Max Sub Array — Kadane's algorithm, the canonical "maximum subarray sum" problem; also a soft entry point into the DP mindset (`0134`) since it has a 1D optimal-substructure recurrence

Starter skeleton: `code/ArrayExercises.java`.
